# Expose standard and custom endpoints safely for exploration
management.endpoints.web.exposure.include=health,info,metrics,mappings,env

# Always show full diagnostic details in health report
management.endpoint.health.show-details=always

# Customize info metadata
info.app.name=Spring Boot Actuator Demo
info.app.description=Exploring actuator features locally
info.app.version=1.0.0
